# github initial setting(ssh)

git config --global user.name ""

git config --global user.email  ""

ssh-keygen

cat  ~/.ssh/id_rsa.pub 



# chancel git add 

git reset HEAD



# chancel git commit

git reset --soft HEAD^
